import './globals.css'

export const metadata = {
  title: 'Artha Job Board - Import History',
  description: 'Admin UI for Job Import History Tracking',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}